<div class="clearfix" id="market-merchants-page">
    <div class="block-header">
        <nav class="breadcrumbs-cust" aria-label="breadcrumb breadcrumb-cust">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="{{LINK merchant_panel}}">{{TR Control panel}}</a>
                </li>
                <li class="breadcrumb-item">
                    <a href="{{LINK merchant_panel/market_merchants}}">
                        {{TR Market sellers}}
                    </a>
                </li>
                <li class="breadcrumb-item active">
                    {{TR Market sellers management}}
                </li>
            </ol>
        </nav>
    </div>
    <div class="row clearfix">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="card ajax-ctrl-table">
                <div class="header">
                    <h2>{{TR List of market sellers}} (<?php echo($hs['total_sellers']); ?>)</h2>
                    <div class="rows-filter">
                        <input type="text" placeholder="{{TR Search by seller name}}" id="search-users">
                        <i class="material-icons search">search</i>
                        <i class="material-icons clear hidden" id="clear-searching-terms">clear</i>
                    </div>
                </div>
                <div class="body table-responsive">
                    <table class="table common-table no-mb" id="users-list">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>{{TR Seller Name}}</th>
                                <th>{{TR E-Mail}}</th>
                                <th>{{TR Country}}</th>
                                <th>{{TR Selling items}}</th>
                                <th>{{TR Last payout}}</th>
                                <th>{{TR Sales}}</th>    
                                <th>{{TR Last seen}}</th>
                                <th>{{TR Action}}</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php if (not_empty($hs['market_sellers'])): ?>
                                <?php foreach ($hs['market_sellers'] as $hs['user_data']): ?>
                                    <?php echo hs_loadpage('market_merchants/includes/user_list_item'); ?>
                                <?php endforeach ?>
                            <?php else: ?>
                                <?php echo hs_loadpage('market_merchants/includes/empty_table'); ?>
                            <?php endif; ?>
                        </tbody>
                    </table>
                    <?php if (not_empty($hs['market_sellers'])): ?>
                        <div class="data-preloader-wrapper hidden" id="table-preloader">
                            <?php echo hs_loadpage('components/data_preloader'); ?>
                        </div>
                        <a href="javascript:void(0);" class="pagination-ctrls prev">
                            <i class="material-icons">chevron_left</i>
                        </a>
                        <a href="javascript:void(0);" class="pagination-ctrls next">
                            <i class="material-icons">chevron_right</i>
                        </a>
                    <?php endif; ?> 
                </div>
            </div>
        </div>
    </div>
</div>
<?php echo hs_loadpage('market_merchants/modals/confirmations/conf_merchant_removal'); ?>
<?php echo hs_loadpage('market_merchants/static/js/market_merchants_script'); ?>